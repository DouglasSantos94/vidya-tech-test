import { View } from "react-native";
import styled from "styled-components/native";

const CustomerFormContent = styled(View)`
  margin-top: 14px;
  width: 100%;
  align-items: center;
  gap: 10px;
`;

export { CustomerFormContent };
